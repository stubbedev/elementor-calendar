# PHP deps (committed: composer.json; not: installed tree)
/vendor/

# Node deps (committed: package.json + build/; not: installed tree)
/node_modules/
.eslintcache
*.tsbuildinfo
npm-debug.log*
yarn-error.log*
.cache/

# Local env / docker overrides
.env
docker-compose.override.yml

# PHPUnit
.phpunit.result.cache
/coverage/

# OS / editor junk
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*~

# WordPress debug output (if ever generated in-tree)
wp-content/debug.log
